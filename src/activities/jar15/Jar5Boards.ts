import Option from '../mat4/entities/option.entity';

export interface Jar5Options {
  girlShoes: Option;
  guyShoes: Option;
  longSleeve: Option;
  pant: Option;
  shirt: Option;
  skirt: Option;
}
